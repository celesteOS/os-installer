using Gtk 4.0;
using Adw 1;

template $PageWrapper : Adw.Bin {
  child: Adw.PreferencesPage {
    Adw.PreferencesGroup {
      Adw.Clamp content {
        maximum-size: 500;
        tightening-threshold: 352;
        hexpand: true;
      }
    }
  };
}
