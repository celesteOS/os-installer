using Gtk 4.0;
using Gio 2.0;

template $ChoicesPage : Box {
  orientation: vertical;
  spacing: 12;

  ListBox list {
    selection-mode: none;
    styles ["boxed-list"]
  }

  $ContinueButton {
    action-name: "win.advance";
  }
}

Gio.ListStore model {}
