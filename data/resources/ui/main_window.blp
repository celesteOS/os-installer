using Gtk 4.0;
using Adw 1;

template $OsInstallerWindow : Adw.ApplicationWindow {
  width-request: 340;
  height-request: 400;
  default-width: 500;
  default-height: 640;

  Stack stack {
    StackPage {
      child: Adw.NavigationView navigation_view {};
    }
  }
}
