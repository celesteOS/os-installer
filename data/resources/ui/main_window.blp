using Gtk 4.0;
using Adw 1;

template $OsInstallerWindow : Adw.ApplicationWindow {
  width-request: 340;
  height-request: 400;
  default-width: 500;
  default-height: 640;

  Stack main_stack {
    transition-type: slide_left_right;
  }
}
