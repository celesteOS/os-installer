using Gtk 4.0;
using Adw 1;

template $ProgressRow : Adw.ActionRow {
  activatable: true;
  focusable: true;
  title: bind template.name;

  [suffix]
  Image {
    icon-name: "go-next-symbolic";
  }
}
