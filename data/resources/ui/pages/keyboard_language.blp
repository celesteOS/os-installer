using Gtk 4.0;
using Adw 1;
using Gio 2.0;

template $KeyboardLanguagePage : Box {
  orientation: vertical;
  spacing: 12;

  ListBox list {
    hexpand: true;
    selection-mode: none;
    row-activated => $language_row_activated();
    styles ["boxed-list"]
  }
}

Gio.ListStore model {}
